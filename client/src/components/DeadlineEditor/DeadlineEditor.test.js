import { render, screen } from '@testing-library/react';
import DeadlineEditor from './index';

test('deadline editor renders', async () => {});

test('form submits on completion', () => {});

test('edit mode: renders edit title and delete button', () => {});

test('edit mode: delete removes deadline', () => {});

test('add mode: renders add title and no delete button', () => {});

test('add mode: adds deadline', () => {});
